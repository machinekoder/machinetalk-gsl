.for socket where (defined(socket.class))
.  socket.load_file("models/$(module:Pascal,lower)/$(socket.class:c).xml")
.  for socket.class as c
.    for c.socket as s
.       socket.type = s.type
.       socket.nature = s.nature
.    endfor
.    delete c
.  endfor
.endfor
